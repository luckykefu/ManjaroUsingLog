---
created: '2024-07-31 '
---
# 
```
mhwd-kernel -li
sudo pacman -Syu virtualbox linux69-virtualbox-host-modules
sudo vboxreload
```

# Install the Extension Pack (Optional)

```
vboxmanage --version

```
## Get extensionpack from VirtualBox
https://www.virtualbox.org/wiki/Downloads
```
sudo vboxmanage extpack install *.vbox-extpack
vboxmanage list extpacks
sudo usermod -aG vboxusers $USER

```
# Running VirtualBox on Manjaro Linux

# How to Uninstall VirtualBox

```
sudo pacman -Rns virtualbox virtualbox-guest-utils
sudo pacman -Rns linux65-virtualbox-host-modules
```
# VBoxGuestAdditions

https://download.v irtualbox.org/virtualbox/

## install iso

### load VBoxGuestAdditions. iso
### install
